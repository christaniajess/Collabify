import { IMailgunClient } from 'mailgun.js/Interfaces';
import { InputFormData, MailgunClientOptions } from 'mailgun.js/Types';
export * as Enums from 'mailgun.js/Enums';
export * from 'mailgun.js/Types';
export * as Interfaces from 'mailgun.js/Interfaces';
export default class Mailgun {
    static get default(): typeof Mailgun;
    private formData;
    constructor(FormData: InputFormData);
    client(options: MailgunClientOptions): IMailgunClient;
}
