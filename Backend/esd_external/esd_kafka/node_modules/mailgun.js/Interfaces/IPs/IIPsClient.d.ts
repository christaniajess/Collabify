import { IpData, IPsListQuery, IpsListResponseBody } from 'mailgun.js/Types/IPs';
export interface IIPsClient {
    list(query: IPsListQuery): Promise<IpsListResponseBody>;
    get(ip: string): Promise<IpData>;
}
