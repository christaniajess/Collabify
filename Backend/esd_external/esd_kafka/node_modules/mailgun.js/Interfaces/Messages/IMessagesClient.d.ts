import { MailgunMessageData, MessagesSendResult } from 'mailgun.js/Types/Messages';
export interface IMessagesClient {
    create(domain: string, data: MailgunMessageData): Promise<MessagesSendResult>;
}
